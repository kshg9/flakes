-- init.lua — managed by hjem (nixos/users/files/yazi/init.lua).
-- The full-border plugin lives at ~/.config/yazi/plugins/full-border.yazi
-- (symlinked from the nixpkgs yaziPlugins package in base.nix).
require("full-border"):setup()